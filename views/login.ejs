<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Login - FormEase</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f9fafb;
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>

  <body class="min-h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md">
      <h1 class="text-2xl font-bold text-center text-blue-800 mb-6">
        Login to CPmail
      </h1>

      <% if (changed) { %>
      <div
        class="p-3 bg-green-100 border border-green-300 text-green-700 rounded mb-5 text-sm"
      >
        ✅ Password changed successfully! Please log in again.
      </div>
      <% } %>

      <form method="POST" action="/login" class="space-y-5">
        <div>
          <label class="block text-gray-700 mb-1 font-medium">Email</label>
          <input
            type="email"
            name="email"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-700 focus:border-transparent"
            placeholder="you@kkumail.com"
          />
        </div>

        <div>
          <label class="block text-gray-700 mb-1 font-medium">Password</label>
          <input
            type="password"
            name="password"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-700 focus:border-transparent"
            placeholder="••••••••"
          />
        </div>

        <div class="pt-2">
          <button
            type="submit"
            class="w-full bg-blue-700 hover:bg-blue-800 text-white py-2 rounded-md font-medium transition-all duration-150"
          >
            Log in
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
