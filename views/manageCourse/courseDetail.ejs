<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>รายละเอียดรายวิชา - <%= course.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <a href="/manageCourse/list?tab=courses" class="btn btn-secondary mb-3">กลับไปรายการวิชา</a>

    <h3>รายละเอียดรายวิชา</h3>
    <table class="table table-bordered">
      <tr><th>รหัสวิชา</th><td><%= course.courseId %></td></tr>
      <tr><th>ชื่อวิชา</th><td><%= course.name %></td></tr>
      <tr><th>รายละเอียด</th><td><%= course.description || '-' %></td></tr>
      <tr><th>หน่วยกิต</th><td><%= course.credits %></td></tr>
      <tr><th>ภาคการศึกษา</th><td><%= course.semester %></td></tr>
    </table>

    <h4 class="mt-4">กลุ่มเรียน</h4>
    <% if (sections.length > 0) { %>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>ชื่อกลุ่ม</th>
            <th>ปีการศึกษา</th>
            <th>ตารางเรียน</th>
            <th>ห้องเรียน</th>
            <th>อาจารย์ผู้สอน</th>
            <th>นักศึกษา</th>
          </tr>
        </thead>
        <tbody>
          <% sections.forEach(sec => { %>
            <tr>
              <td><%= sec.name %></td>
              <td><%= sec.year %></td>
              <td><%= sec.schedule || '-' %></td>
              <td><%= sec.room || '-' %></td>
              <td>
                <%= sec.lecturers.length %> (<%= sec.lecturers.map(l => l.name).join(', ') %>)
              </td>
              <td>
                <%= sec.students.length %> / <%= sec.maxStudents %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <p>ไม่พบกลุ่มเรียนสำหรับรายวิชานี้</p>
    <% } %>
  </div>
</body>
</html>