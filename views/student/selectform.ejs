<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ยื่นฟอร์ม - เลือกเทมเพลต - FormEase</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'kku-blue': '#1e40af',
                        'kku-blue-light': '#3b82f6',
                        'kku-blue-dark': '#1e3a8a'
                    }
                }
            }
        }
    </script>
</head>

    <%- include('../partials/navbar', { activeMenu: 'submit' }) %>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-6">
        <!-- Page Header -->
        <div class="mb-6">
            <h2 class="text-2xl font-bold text-gray-900 mb-2">ยื่นฟอร์มใหม่</h2>
            <p class="text-gray-600">กรอกแบบฟอร์มด้านล่างเพื่อยื่นคำขอไปยังฝ่ายบริหารคณะ</p>
        </div>

        <!-- Progress Indicator -->
        <div class="mb-8">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <div class="flex items-center text-kku-blue">
                        <div
                            class="rounded-full transition duration-500 ease-in-out h-10 w-10 py-3 border-2 border-kku-blue bg-kku-blue text-white text-center flex items-center justify-center">
                            <span class="text-sm font-medium">1</span>
                        </div>
                        <div class="ml-2 text-sm font-medium">เลือกเทมเพลต</div>
                    </div>
                </div>
                <div class="flex-auto border-t-2 transition duration-500 ease-in-out border-gray-300 mx-4"></div>
                <div class="flex items-center">
                    <div class="flex items-center text-gray-500">
                        <div
                            class="rounded-full transition duration-500 ease-in-out h-10 w-10 py-3 border-2 border-gray-300 text-center flex items-center justify-center">
                            <span class="text-sm font-medium">2</span>
                        </div>
                        <div class="ml-2 text-sm font-medium">กรอกรายละเอียด</div>
                    </div>
                </div>
                <div class="flex-auto border-t-2 transition duration-500 ease-in-out border-gray-300 mx-4"></div>
                <div class="flex items-center">
                    <div class="flex items-center text-gray-500">
                        <div
                            class="rounded-full transition duration-500 ease-in-out h-10 w-10 py-3 border-2 border-gray-300 text-center flex items-center justify-center">
                            <span class="text-sm font-medium">3</span>
                        </div>
                        <div class="ml-2 text-sm font-medium">ตรวจสอบและส่ง</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Form Container -->
        <div class="bg-white rounded-lg shadow-lg p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-6">เลือกเทมเพลตฟอร์ม</h3>

            <!-- Category Selection -->
            <div class="mb-6">
                <form method="get" action="/student/forms/submit">
                    <label for="category" class="block text-sm font-medium text-gray-700 mb-2">
                        ประเภทฟอร์ม <span class="text-red-500">*</span>
                    </label>
                    <select id="category" name="category" onchange="this.form.submit()"
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-kku-blue focus:border-transparent transition-all">
                        <option value="">-- เลือกประเภทฟอร์ม --</option>
                        <% (categories||[]).forEach(c=> { %>
                            <option value="<%= c %>" <%=selCategory===c ? 'selected' : '' %>>
                                <%= c %>
                            </option>
                            <% }) %>
                    </select>
                    <noscript>
                        <button type="submit"
                            class="mt-2 bg-gray-500 text-white px-4 py-2 rounded-md hover:bg-gray-600 transition-colors">
                            โหลดรายการฟอร์ม
                        </button>
                    </noscript>
                </form>
            </div>

            <!-- Template Selection -->
            <% if (selCategory && templates && templates.length) { %>
                <div class="mb-6">
                    <form method="get" action="/student/forms/submit">
                        <input type="hidden" name="category" value="<%= selCategory %>" />
                        <label for="templateId" class="block text-sm font-medium text-gray-700 mb-2">
                            เลือกฟอร์มที่ต้องการยื่น <span class="text-red-500">*</span>
                        </label>
                        <select id="templateId" name="templateId" onchange="this.form.submit()"
                            class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-kku-blue focus:border-transparent transition-all">
                            <option value="">-- เลือกฟอร์ม --</option>
                            <% templates.forEach(t=> { %>
                                <option value="<%= t._id %>" <%=selTemplateId===String(t._id) ? 'selected' : '' %>>
                                    <%= t.title %>
                                </option>
                                <% }) %>
                        </select>
                        <noscript>
                            <button type="submit"
                                class="mt-2 bg-gray-500 text-white px-4 py-2 rounded-md hover:bg-gray-600 transition-colors">
                                ดูรายละเอียด
                            </button>
                        </noscript>
                    </form>
                </div>
                <% } %>

                    <!-- Template Details -->
                    <% if (selectedTemplate) { %>
                        <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-6">
                            <h4 class="font-semibold text-blue-900 text-lg mb-3">
                                <%= selectedTemplate.title %>
                            </h4>

                            <div class="mb-4">
                                <p class="text-blue-800 text-sm">
                                    <%= selectedTemplate.description || 'ไม่มีคำอธิบาย' %>
                                </p>
                            </div>

                            <div class="border-t border-blue-200 pt-4">
                                <h5 class="font-medium text-blue-900 text-sm mb-3 flex items-center">
                                    <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                        <path fill-rule="evenodd"
                                            d="M4 4a2 2 0 012-2h4.586A2 2 0 0112 2.586L15.414 6A2 2 0 0116 7.414V16a2 2 0 01-2 2H6a2 2 0 01-2-2V4zm2 6a1 1 0 011-1h6a1 1 0 110 2H7a1 1 0 01-1-1zm1 3a1 1 0 100 2h6a1 1 0 100-2H7z"
                                            clip-rule="evenodd"></path>
                                    </svg>
                                    เอกสารที่ต้องใช้ในการยื่นฟอร์ม
                                </h5>

                                <% if ((requiredDocs||[]).length===0) { %>
                                    <p class="text-blue-700 text-sm ml-7">ไม่จำเป็นต้องแนบเอกสาร</p>
                                    <% } else { %>
                                        <ul class="space-y-2 ml-7">
                                            <% requiredDocs.forEach(d=> { %>
                                                <li class="text-blue-800 text-sm flex items-start">
                                                    <svg class="w-4 h-4 mr-2 mt-0.5 flex-shrink-0" fill="currentColor"
                                                        viewBox="0 0 20 20">
                                                        <path fill-rule="evenodd"
                                                            d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                                                            clip-rule="evenodd"></path>
                                                    </svg>
                                                    <span>
                                                        <%= d.label %>
                                                            <% if (d.required) { %>
                                                                <span
                                                                    class="inline-block ml-2 bg-red-100 text-red-800 text-xs px-2 py-0.5 rounded-full">จำเป็น</span>
                                                                <% } else { %>
                                                                    <span
                                                                        class="inline-block ml-2 bg-gray-100 text-gray-800 text-xs px-2 py-0.5 rounded-full">ไม่บังคับ</span>
                                                                    <% } %>
                                                    </span>
                                                </li>
                                                <% }) %>
                                        </ul>
                                        <% } %>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="flex justify-between items-center pt-4 border-t">
                            <a href="/student"
                                class="text-gray-600 hover:text-gray-800 font-medium flex items-center transition-colors">
                                <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                                </svg>
                                กลับหน้าหลัก
                            </a>
                            <form method="get" action="/student/forms/submit/details">
                                <input type="hidden" name="templateId" value="<%= String(selectedTemplate._id) %>" />
                                <button type="submit"
                                    class="bg-kku-blue text-white px-6 py-2.5 rounded-md hover:bg-kku-blue-dark transition-colors font-medium shadow-md hover:shadow-lg flex items-center">
                                    ถัดไป: กรอกรายละเอียดฟอร์ม
                                    <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
                                    </svg>
                                </button>
                            </form>
                        </div>
                        <% } else if (selCategory && templates && templates.length) { %>
                            <!-- Show back button when category is selected but no template -->
                            <div class="flex justify-start pt-4 border-t">
                                <a href="/student"
                                    class="text-gray-600 hover:text-gray-800 font-medium flex items-center transition-colors">
                                    <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                                    </svg>
                                    กลับหน้าหลัก
                                </a>
                            </div>
                            <% } else { %>
                                <!-- Show back button on initial load -->
                                <div class="flex justify-start pt-4 border-t">
                                    <a href="/student"
                                        class="text-gray-600 hover:text-gray-800 font-medium flex items-center transition-colors">
                                        <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                                        </svg>
                                        กลับหน้าหลัก
                                    </a>
                                </div>
                                <% } %>
        </div>
    </main>
</body>

</html>