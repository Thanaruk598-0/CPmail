<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ยื่นฟอร์ม - กรอกรายละเอียด - FormEase</title>
    <script src="https://cdn.tailwindcss.com"></script><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'kku-blue': '#1e40af',
                        'kku-blue-light': '#3b82f6',
                        'kku-blue-dark': '#1e3a8a'
                    }
                }
            }
        }
    </script>
</head>

<body class="bg-gray-50 font-sans">
    
    <%- include('../partials/navbar', { activeMenu: 'submit' }) %>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-6">
        <!-- Page Header -->
        <div class="mb-6">
            <h2 class="text-2xl font-bold text-gray-900 mb-2">ยื่นฟอร์มใหม่</h2>
            <p class="text-gray-600">กรอกรายละเอียดฟอร์มให้ครบถ้วน</p>
        </div>

        <!-- Progress Indicator -->
        <div class="mb-8">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <div class="flex items-center text-kku-blue">
                        <div
                            class="rounded-full transition duration-500 ease-in-out h-10 w-10 border-2 border-kku-blue bg-kku-blue text-white text-center flex items-center justify-center">
                            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd"
                                    d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                                    clip-rule="evenodd"></path>
                            </svg>
                        </div>
                        <div class="ml-2 text-sm font-medium">เลือกเทมเพลต</div>
                    </div>
                </div>
                <div class="flex-auto border-t-2 border-kku-blue mx-4"></div>
                <div class="flex items-center">
                    <div class="flex items-center text-kku-blue">
                        <div
                            class="rounded-full transition duration-500 ease-in-out h-10 w-10 border-2 border-kku-blue bg-kku-blue text-white text-center flex items-center justify-center">
                            <span class="text-sm font-medium">2</span>
                        </div>
                        <div class="ml-2 text-sm font-medium">กรอกรายละเอียด</div>
                    </div>
                </div>
                <div class="flex-auto border-t-2 border-gray-300 mx-4"></div>
                <div class="flex items-center">
                    <div class="flex items-center text-gray-500">
                        <div
                            class="rounded-full transition duration-500 ease-in-out h-10 w-10 border-2 border-gray-300 text-center flex items-center justify-center">
                            <span class="text-sm font-medium">3</span>
                        </div>
                        <div class="ml-2 text-sm font-medium">ตรวจสอบและส่ง</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Form Container -->
        <div class="bg-white rounded-lg shadow-lg p-6">
            <!-- Template Info -->
            <div class="mb-6 pb-6 border-b">
                <div class="flex items-start justify-between">
                    <div>
                        <h3 class="text-xl font-semibold text-gray-900 mb-2">
                            <%= template?.title %>
                        </h3>
                        <p class="text-gray-600 text-sm">
                            <%= template?.description || '-' %>
                        </p>
                    </div>
                    <span class="bg-kku-blue-light text-white text-xs px-3 py-1 rounded-full">
                        <%= template?.category || 'ฟอร์มทั่วไป' %>
                    </span>
                </div>
            </div>

            <!-- Student Information (Auto-filled) -->
            <div class="mb-6 bg-gray-50 rounded-lg p-5">
                <h4 class="font-semibold text-gray-900 mb-4 flex items-center">
                    <svg class="w-5 h-5 mr-2 text-kku-blue" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z"
                            clip-rule="evenodd"></path>
                    </svg>
                    ข้อมูลผู้ยื่น
                </h4>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div class="flex items-start">
                        <span class="text-gray-600 text-sm font-medium w-32">ชื่อ:</span>
                        <span class="text-gray-900 text-sm font-semibold">
                            <%= student?.name %>
                        </span>
                    </div>
                    <div class="flex items-start">
                        <span class="text-gray-600 text-sm font-medium w-32">รหัสนักศึกษา:</span>
                        <span class="text-gray-900 text-sm font-semibold">
                            <%= student?.universityId %>
                        </span>
                    </div>
                    <div class="flex items-start">
                        <span class="text-gray-600 text-sm font-medium w-32">อีเมล:</span>
                        <span class="text-gray-900 text-sm">
                            <%= student?.email %>
                        </span>
                    </div>
                    <div class="flex items-start">
                        <span class="text-gray-600 text-sm font-medium w-32">สาขาวิชา:</span>
                        <span class="text-gray-900 text-sm">
                            <%= student?.major %>
                        </span>
                    </div>
                </div>
            </div>

            <!-- Course Selection -->
            <div class="mb-6">
                <form method="get" action="/student/forms/submit/details" class="space-y-4">
                    <input type="hidden" name="templateId" value="<%= String(template._id) %>" />

                    <div>
                        <label for="courseId" class="block text-sm font-medium text-gray-700 mb-2">
                            เลือกรายวิชา <span class="text-red-500">*</span>
                        </label>
                        <div class="flex gap-2">
                            <select id="courseId" name="courseId"
                                class="flex-1 px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-kku-blue focus:border-transparent transition-all">
                                <option value="">-- เลือกรายวิชาที่ลงทะเบียนไว้ --</option>
                                <% (enrolledCourses||[]).forEach(c=> { %>
                                    <option value="<%= c._id %>" <%=String(c._id)===String(selCourseId) ? 'selected'
                                        : '' %>>
                                        <%= c.courseId %> - <%= c.name %>
                                    </option>
                                    <% }) %>
                            </select>
                            <button type="submit"
                                class="bg-kku-blue text-white px-6 py-2 rounded-md hover:bg-kku-blue-dark transition-colors font-medium whitespace-nowrap">
                                เลือกวิชา
                            </button>
                        </div>
                    </div>
                </form>
            </div>

            <% if (selCourseId) { %>
                <!-- Course Information -->
                <div class="mb-6 bg-blue-50 border border-blue-200 rounded-lg p-5">
                    <h4 class="font-semibold text-blue-900 mb-4 flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
                            <path
                                d="M10.394 2.08a1 1 0 00-.788 0l-7 3a1 1 0 000 1.84L5.25 8.051a.999.999 0 01.356-.257l4-1.714a1 1 0 11.788 1.838L7.667 9.088l1.94.831a1 1 0 00.787 0l7-3a1 1 0 000-1.838l-7-3zM3.31 9.397L5 10.12v4.102a8.969 8.969 0 00-1.05-.174 1 1 0 01-.89-.89 11.115 11.115 0 01.25-3.762zM9.3 16.573A9.026 9.026 0 007 14.935v-3.957l1.818.78a3 3 0 002.364 0l5.508-2.361a11.026 11.026 0 01.25 3.762 1 1 0 01-.89.89 8.968 8.968 0 00-5.35 2.524 1 1 0 01-1.4 0zM6 18a1 1 0 001-1v-2.065a8.935 8.935 0 00-2-.712V17a1 1 0 001 1z">
                            </path>
                        </svg>
                        ข้อมูลรายวิชา
                    </h4>
                    <div class="space-y-3">
                        <div class="flex items-start">
                            <span class="text-blue-700 text-sm font-medium w-32">รายวิชา:</span>
                            <span class="text-blue-900 text-sm font-semibold">
                                <%= selectedCourse?.courseId %> - <%= selectedCourse?.name %>
                            </span>
                        </div>
                        <div class="flex items-start">
                            <span class="text-blue-700 text-sm font-medium w-32">Section:</span>
                            <span class="text-blue-900 text-sm">
                                <%= selectedSection?.name || '-' %>
                            </span>
                        </div>
                        <div class="flex items-start">
                            <span class="text-blue-700 text-sm font-medium w-32">อาจารย์:</span>
                            <div class="flex-1">
                                <% if ((lecturers||[]).length===0) { %>
                                    <span class="text-blue-700 text-sm italic">ไม่พบข้อมูล</span>
                                    <% } else { %>
                                        <ul class="space-y-1">
                                            <% lecturers.forEach(l=> { %>
                                                <li class="text-blue-900 text-sm">
                                                    <%= l.name %>
                                                        <span class="text-blue-700">(<%= l.email %>)</span>
                                                </li>
                                                <% }) %>
                                        </ul>
                                        <% } %>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Form Details -->
                <form method="post" action="/student/forms/submit/preview">
                    <input type="hidden" name="templateId" value="<%= String(template._id) %>" />
                    <input type="hidden" name="courseId" value="<%= String(selCourseId) %>" />

                    <div class="mb-6">
                        <h4 class="font-semibold text-gray-900 mb-4 flex items-center">
                            <svg class="w-5 h-5 mr-2 text-kku-blue" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd"
                                    d="M4 4a2 2 0 012-2h4.586A2 2 0 0112 2.586L15.414 6A2 2 0 0116 7.414V16a2 2 0 01-2 2H6a2 2 0 01-2-2V4z"
                                    clip-rule="evenodd"></path>
                            </svg>
                            รายละเอียดฟอร์ม
                        </h4>

                        <% if ((inputFields||[]).length===0) { %>
                            <div class="bg-gray-50 rounded-lg p-4 text-center">
                                <p class="text-gray-500 text-sm">ไม่มีฟิลด์ที่ต้องกรอกเพิ่มเติม</p>
                            </div>
                            <% } else { %>
                                <div class="space-y-4">
                                    <% inputFields.forEach(f=> {
                                        const key = (f.name || f.key || f.label || '').trim();
                                        const type = (f.type || 'text').toLowerCase();
                                        const required = f.required ? 'required' : '';
                                        const requiredLabel = f.required ? '<span class="text-red-500">*</span>' : '';
                                        %>
                                        <div>
                                            <label class="block text-sm font-medium text-gray-700 mb-2">
                                                <%= f.label || key %> <%- requiredLabel %>
                                                        <% if (f.required) { %>
                                                            <span
                                                                class="text-xs text-gray-500 font-normal">(จำเป็น)</span>
                                                            <% } %>
                                            </label>

                                            <% if (type==='textarea' ) { %>
                                                <textarea name="data_<%= key %>" <%=required %>
                                                rows="4"
                                                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-kku-blue focus:border-transparent transition-all"
                                                placeholder="กรอก<%= f.label || key %>"></textarea>
                                                <% } else if (type==='date' ) { %>
                                                    <input type="date" name="data_<%= key %>" <%=required %>
                                                    class="w-full px-4 py-2 border border-gray-300 rounded-md
                                                    focus:outline-none focus:ring-2 focus:ring-kku-blue
                                                    focus:border-transparent transition-all" />
                                                    <% } else if (type==='file' ) { %>
                                                        <input type="text" name="data_<%= key %>" <%=required %>
                                                        placeholder="ระบุชื่อไฟล์หรือแนบภายหลัง"
                                                        class="w-full px-4 py-2 border border-gray-300 rounded-md
                                                        focus:outline-none focus:ring-2 focus:ring-kku-blue
                                                        focus:border-transparent transition-all" />
                                                        <p class="text-xs text-gray-500 mt-1">
                                                            <svg class="w-4 h-4 inline mr-1" fill="currentColor"
                                                                viewBox="0 0 20 20">
                                                                <path fill-rule="evenodd"
                                                                    d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z"
                                                                    clip-rule="evenodd"></path>
                                                            </svg>
                                                            การอัปโหลดไฟล์จะเพิ่มในเวอร์ชันถัดไป
                                                        </p>
                                                        <% } else { %>
                                                            <input type="text" name="data_<%= key %>" <%=required %>
                                                            placeholder="กรอก<%= f.label || key %>"
                                                                class="w-full px-4 py-2 border border-gray-300
                                                                rounded-md focus:outline-none focus:ring-2
                                                                focus:ring-kku-blue focus:border-transparent
                                                                transition-all" />
                                                                <% } %>

                                                                    <% if (f.help) { %>
                                                                        <p class="text-xs text-gray-500 mt-1">
                                                                            <%= f.help %>
                                                                        </p>
                                                                        <% } %>
                                        </div>
                                        <% }) %>
                                </div>
                                <% } %>
                    </div>

                    <!-- Additional Reason -->
                    <div class="mb-6">
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            เหตุผล/คำชี้แจงเพิ่มเติม (ถ้ามี)
                        </label>
                        <textarea name="reason" rows="4"
                            class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-kku-blue focus:border-transparent transition-all"
                            placeholder="อธิบายเหตุผลประกอบการยื่นฟอร์ม (ไม่บังคับ)"></textarea>
                    </div>

                    <!-- Action Buttons -->
                    <div class="flex justify-between items-center pt-6 border-t">
                        <a href="/student/forms/submit?category=<%= template.category %>&templateId=<%= String(template._id) %>"
                            class="text-gray-600 hover:text-gray-800 font-medium flex items-center transition-colors">
                            <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                            </svg>
                            กลับ: เลือกเทมเพลต
                        </a>
                        <button type="submit"
                            class="bg-kku-blue text-white px-6 py-2.5 rounded-md hover:bg-kku-blue-dark transition-colors font-medium shadow-md hover:shadow-lg flex items-center">
                            ถัดไป: ตัวอย่างฟอร์ม
                            <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
                            </svg>
                        </button>
                    </div>
                </form>
                <% } %>

                    <!-- Cancel Button -->
                    <% if (!selCourseId) { %>
                        <div class="flex justify-start pt-6 border-t">
                            <a href="/student"
                                class="text-red-600 hover:text-red-700 font-medium flex items-center transition-colors">
                                <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M6 18L18 6M6 6l12 12"></path>
                                </svg>
                                ยกเลิกและกลับ Dashboard
                            </a>
                        </div>
                        <% } %>
        </div>
    </main>
</body>

</html>