<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>Available Templates</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body class="p-4 bg-light">

<%- include('../../partials/navbar', { user: currentUser, activeMenu: 'forms' }) %>

<div class="container">
  <h2 class="mb-4">แบบฟอร์มที่เปิดให้ใช้งาน</h2>

  <div class="row">
    <% if (templates.length === 0) { %>
      <p class="text-muted">ยังไม่มีเทมเพลตที่เปิดให้ใช้งาน</p>
    <% } else { %>
      <% templates.forEach(t => { %>
        <div class="col-md-4">
          <div class="card mb-3 shadow-sm">
            <div class="card-body">
              <h5 class="card-title"><%= t.title %></h5>
              <p class="card-text"><%= t.description || '-' %></p>
              <p class="small text-muted">หมวดหมู่: <%= t.category || '-' %></p>
              <a href="/forms/new/<%= t._id %>" class="btn btn-primary">กรอกฟอร์มนี้</a>

            </div>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>
</div>

</body>
</html>
